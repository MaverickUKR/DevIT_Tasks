// const eventEmitter = new MyEventEmitter()

// const listenner = (data) => (
//     console.log(data)
// )

// const EVENT_NAME = 'some event'

// eventEmitter.subscribe(EVENT_NAME, listenner)

// eventEmitter.emit(EVENT_NAME, {value: 'Hello World!'})

// eventEmitter.unsubscribe(EVENT_NAME, listenner)

const x = 1;
const y = 2;
const x1 = 2;
const y1 = 3;

const xx1 = x1 - x;
